apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubemcp.fullname" . }}
  labels:
    {{- include "kubemcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubemcp.selectorLabels" . | nindent 4 }}
