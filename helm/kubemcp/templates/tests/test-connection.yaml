apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubemcp.fullname" . }}-test-connection"
  labels:
    {{- include "kubemcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubemcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
